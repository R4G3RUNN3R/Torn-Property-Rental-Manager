{
  "name": "torn-property-rental-manager",
  "version": "0.3.3",
  "private": true,
  "description": "Standalone Torn rental manager for configurable exact-match market pricing and explicit property listing",
  "scripts": {
    "build": "node scripts/build-userscript.js",
    "build:check": "node scripts/build-userscript.js --check",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/*.js scripts/build-userscript.js R4G3RUNN3R-Property-Rental-Manager.user.js",
    "verify": "npm run build:check && npm test && npm run check"
  },
  "devDependencies": {
    "jsdom": "29.1.1"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
