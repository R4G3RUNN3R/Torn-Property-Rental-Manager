{
  "name": "torn-property-rental-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Torn property rental market scanner and lease preparation userscript",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/*.js"
  },
  "engines": {
    "node": ">=20"
  }
}
